$CLOCK_FONT = Inter Variable
$FONT = Inter Variable
$MONOSPACE_FONT = CaskaydiaCove Nerd Font Propo

animations {
  animation = fadeIn, 1, 10
  animation = fadeOut, 1, 10
}

background {
  path = $BACKGROUND_PATH
  color = $primary_container
  blur_passes = 0
  blur_size = 0
}

# --- Main container --- #

shape {
  size = 400, 140
  color = $surface
  shadow_passes = 1
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 30, 30
  border_size = 2
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = 24
  zindex = 2
}

# --- User image --- #

image {
  shadow_passes = 1
  shadow_size = 3
  shadow_color = $shadow
  shadow_boost = 0.75
  path = $PROFILE_IMAGE
  reload_cmd = $SCRIPTS_PATH/user-picture.sh
  reload_time = 1
  border_size = 0
  border_color = $primary
  size = 120
  halign = left
  valign = bottom
  position = 42, 42
  rounding = 14
  zindex = 3
}

# --- User label --- #

label {
  color = $on_surface
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = cmd[update:0] $SCRIPTS_PATH/truncate.sh "$USER" 12
  font_size = 20
  font_family = $FONT Bold
  halign = left
  valign = bottom
  position = 182, 122
  zindex = 3
}

label {
  color = $on_surface_variant
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = cmd[update:0] $SCRIPTS_PATH/truncate.sh "$(cat /etc/hostname)" 15
  font_size = 14
  font_family = $FONT
  halign = left
  valign = bottom
  position = 182, 102
  zindex = 3
}

# --- Input field --- #

input-field {
  size = 225, 30
  outline_thickness = 2
  dots_size = 0.25
  dots_spacing = 0.4
  dots_center = false
  dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
  outer_color = $outline_variant
  inner_color = $primary_container
  font_color = $on_primary_container
  font_family = $FONT
  fade_on_empty = false
  fade_timeout = 2000
  placeholder_text = Enter password # Text rendered in the input box when it's empty.
  hide_input = false
  rounding = -1 # -1 means complete rounding (circle/oval)
  check_color = $primary
  check_text = Authenticating...
  fail_color = $error # if authentication failed, changes outer_color and fail message color
  fail_text = <b>$FAIL ($ATTEMPTS)</b> # can be set to empty
  fail_transition = 200 # transition time in ms between normal outer_color and fail_color
  capslock_color = $tertiary
  numlock_color = $outline_variant
  bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
  invert_numlock = false # change color if numlock is off
  swap_font_color = false
  position = 182, 52
  halign = left
  valign = bottom
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 0.75
  zindex = 4
}

# --- Date container --- #

shape {
  size = 320, 160
  color = $surface
  shadow_passes = 1
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 30, 190
  border_size = 2
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = 24
  zindex = 2
}

# --- Time container --- #

shape {
  size = 120, 140
  color = $primary_container
  shadow_passes = 0
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 42, 202
  border_size = 0
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = 14
  zindex = 3
}

# --- Time label --- #

label {
  color = $primary
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = cmd[update:1000] echo "<span font_features='tnum'>$(date '+%H')</span>"
  font_size = 48
  font_family = $CLOCK_FONT Black
  halign = left
  valign = bottom
  position = 61, 262
  zindex = 4
}

label {
  color = $primary
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = cmd[update:1000] echo "<span font_features='tnum'>$(date '+%M')</span>"
  font_size = 48
  font_family = $CLOCK_FONT Black
  halign = left
  valign = bottom
  position = 61, 202
  zindex = 4
}

# --- Date label --- #

label {
  color = $on_surface
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = cmd[update:1000] echo "<span font_features='tnum'>$(date '+%a %d')</span>"
  font_size = 24
  font_family = $FONT Bold
  halign = left
  valign = bottom
  position = 182, 292
  zindex = 4
}

label {
  color = $on_surface_variant
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = cmd[update:1000] echo "<span font_features='tnum'>$(date '+%b %Y')</span>"
  font_size = 16
  font_family = $FONT
  halign = left
  valign = bottom
  position = 182, 262
  zindex = 4
}

# --- Uptime divider --- #

shape {
  size = 170, 2
  color = $outline_variant
  shadow_passes = 0
  shadow_size = 1
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 172, 247
  border_size = 0
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = 24
  zindex = 4
}

# --- Uptime label --- #

label {
  color = $on_surface
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = cmd[update:1000] echo "   $($SCRIPTS_PATH/uptime.sh)"
  font_size = 14
  font_family = $FONT
  halign = left
  valign = bottom
  position = 182, 212
  zindex = 4
}

# --- Power button container --- #

shape {
  size = 60, 160
  color = $surface
  shadow_passes = 1
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 370, 190
  border_size = 2
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = -1
  zindex = 2
}

# --- Power buttons --- #


shape {
  size = 40, 40
  color = $error_container
  shadow_passes = 0
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 382, 302
  border_size = 0
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = -1
  zindex = 3
}

label {
  color = $on_error_container
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = 󰐥
  font_size = 22
  font_family = $MONOSPACE_FONT
  halign = left
  valign = bottom
  position = 392, 306
  zindex = 4
  onclick = systemctl poweroff
}

shape {
  size = 40, 40
  color = $secondary_container
  shadow_passes = 0
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 382, 252
  border_size = 0
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = -1
  zindex = 3
}

label {
  color = $on_secondary_container
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = 󰍃
  font_size = 21
  font_family = $MONOSPACE_FONT
  halign = left
  valign = bottom
  position = 394, 256
  zindex = 4
  onclick = niri msg action quit -s
}

shape {
  size = 40, 40
  color = $tertiary_container
  shadow_passes = 0
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 382, 202
  border_size = 0
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = -1
  zindex = 3
}

label {
  color = $on_tertiary_container
  shadow_passes = 0
  shadow_size = 2
  shadow_color = $shadow
  shadow_boost = 1.2
  text = 󰤄
  font_size = 22
  font_family = $MONOSPACE_FONT
  halign = left
  valign = bottom
  position = 393, 206
  zindex = 4
  onclick = systemctl suspend
}

# --- Media container --- #

shape {
  size = 400, 140
  color = $surface
  shadow_passes = 1
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 30, 370
  border_size = 2
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = 24
  zindex = 2
}

# --- Media thumbnail --- #

image {
  shadow_passes = 1
  shadow_size = 3
  shadow_color = $shadow
  shadow_boost = 0.75
  reload_cmd = $SCRIPTS_PATH/mpris-art.sh $EMPTY_IMAGE
  reload_time = 1
  border_size = 0
  border_color = $outline
  size = 120
  halign = left
  valign = bottom
  position = 42, 382
  rounding = 14
  zindex = 5
}

# --- Media thumbnail (no active media) --- #

shape {
  size = 120, 120
  color = $secondary_container
  shadow_passes = 0
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = 42, 382
  border_size = 0
  border_color = $outline_variant
  halign = left
  valign = bottom
  rounding = 14
  zindex = 3
}

label {
  text = 󰝛
  color = $outline
  shadow_passes = 0
  shadow_size = 1
  shadow_color = $shadow
  shadow_boost = 1.2
  font_size = 60
  font_family = $MONOSPACE_FONT
  position = 67, 392
  halign = left
  valign = bottom
  zindex = 4
  onclick = playerctl next && pkill -USR2 hyprlock
}

# --- Media label --- #

label {
  color = $on_surface
  shadow_passes = 0
  shadow_size = 1
  shadow_color = $shadow
  shadow_boost = 0.75
  text = cmd[update:1000:1] if [[ "$(playerctl status)" && "$(playerctl status)" != "Stopped" ]]; then $SCRIPTS_PATH/truncate.sh "$(playerctl metadata xesam:title)" 15; else echo "No media playing"; fi
  font_size = 15
  font_family = $FONT Bold
  position = 182, 462
  halign = left
  valign = bottom
  zindex = 4
}

label {
  color = $on_surface_variant
  shadow_passes = 0
  shadow_size = 1
  shadow_color = $shadow
  shadow_boost = 0.75
  text = cmd[update:1000:1] [[ "$(playerctl status)" && "$(playerctl status)" != "Stopped" ]] && $SCRIPTS_PATH/truncate.sh "$(playerctl metadata xesam:artist)" 20
  font_size = 12
  font_family = $FONT SemiLight
  position = 182, 442
  halign = left
  valign = bottom
  zindex = 4
}

# -- Media control buttons --- #

label {
  text = 󰼨
  color = $secondary
  shadow_passes = 0
  shadow_size = 1
  shadow_color = $shadow
  shadow_boost = 1.2
  font_size = 24
  font_family = $MONOSPACE_FONT
  position = 242, 387
  halign = left
  valign = bottom
  zindex = 4
  onclick = playerctl previous && pkill -USR2 hyprlock
}

label {
  text = cmd[update:1000:1] if [ "$(playerctl status)" = "Playing" ]; then echo "󰏥"; else echo "󰐍"; fi
  color = $primary
  shadow_passes = 0
  shadow_size = 1
  shadow_color = $shadow
  shadow_boost = 1.2
  font_size = 30
  font_family = $MONOSPACE_FONT
  position = 282, 382
  halign = left
  valign = bottom
  zindex = 4
  onclick = playerctl play-pause && pkill -USR2 hyprlock
}

label {
  text = 󰼧
  color = $secondary
  shadow_passes = 0
  shadow_size = 1
  shadow_color = $shadow
  shadow_boost = 1.2
  font_size = 24
  font_family = $MONOSPACE_FONT
  position = 337, 387
  halign = left
  valign = bottom
  zindex = 4
  onclick = playerctl next && pkill -USR2 hyprlock
}

# --- Lock icon --- #

shape {
  size = 60, 60
  color = $primary
  shadow_passes = 0
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = -40, 80 
  border_size = 0
  border_color = $outline_variant
  halign = right
  valign = bottom
  rounding = 4
  zindex = 3
  rotate = 15
}

shape {
  size = 60, 60
  color = $primary
  shadow_passes = 0
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = -34, 74
  border_size = 0
  border_color = $outline_variant
  halign = right
  valign = bottom
  rounding = 4
  zindex = 3
  rotate = 45
}

shape {
  size = 60, 60
  color = $primary
  shadow_passes = 0
  shadow_size = 6
  shadow_color = $shadow
  shadow_boost = 0.75
  position = -40, 80
  border_size = 0
  border_color = $outline_variant
  halign = right
  valign = bottom
  rounding = 4
  zindex = 3
  rotate = 75
}

shape {
  size = 50, 50
  color = $primary
  shadow_passes = 2
  shadow_size = 5
  shadow_color = $shadow
  shadow_boost = 2.0
  position = -53, 93
  border_size = 0
  border_color = $outline_variant
  halign = right
  valign = bottom
  rounding = -1
  zindex = 2
}

label {
  text = 󰌾
  color = $on_primary
  shadow_passes = 0
  shadow_size = 1
  shadow_color = $shadow
  shadow_boost = 1.2
  font_size = 36
  font_family = $MONOSPACE_FONT
  position = -62, 91
  halign = right
  valign = bottom
  zindex = 4
}

# --- Greeting label --- #

label {
  text = cmd[update:60000] echo "Good "$(if [ $(date +%-H) -lt 12 ]; then echo "morning"; elif [ $(date +%-H) -lt 18 ]; then echo "afternoon"; else echo "evening"; fi)""
  color = $on_surface_dark
  shadow_passes = 1
  shadow_size = 1
  shadow_color = $shadow
  shadow_boost = 1.0
  font_size = 24
  font_family = $FONT
  position = -30, 30
  halign = right
  valign = bottom
  zindex = 4
}
