# HyDE default layout
# Wallbash generated layout

# Local Variables
$fontFamily = CaskaydiaCove Nerd Font SemiLight
$fn_greet=echo "Good $(date +%H | awk '{if ($1 < 12) print "Morning"; else if ($1 < 18) print "Afternoon"; else print "Evening"}'), ${USER}"
$white = rgb(250,250,250)

# GENERAL
general {
    grace = 0
}

animations {
    bezier = linear, 1, 1, 0, 0
    animation = fade, 1, 3.0, linear
}

# BACKGROUND
background {
    monitor =
    path = $BACKGROUND_PATH
    blur_passes = 3
    blur_size = 3
}

# TIME
label {
  monitor =
  text = cmd[update:360000] date +"%H"
  color = $primary
  font_size = 200
  font_family = CaskaydiaCove Nerd Font ExtraBold
  position = -3%, -7%
  halign = center
  valign = top
  zindex = 5
}

label {
  monitor =
  text = cmd[update:6000] date +"%M"
  color = $secondary
  font_size = 200
  font_family = CaskaydiaCove Nerd Font ExtraBold
  position = 3%, -27%
  halign = center
  valign = top
  zindex = 5
}

#label {
#  monitor =
#  text = /
#  color = $on_surface
#  font_size = 200
#  font_family = CaskaydiaCove Nerd Font Light
#  position = -0%, -18%
#  halign = center
#  valign = top
#  zindex = 5
#  rotate = -30
#}

# DATE
label {
  monitor =
  text = cmd[update:43200000] date +"%A, %d %B %Y"
  color = $white
  font_size = 20
  font_family = CaskaydiaCove Nerd Font Bold
  position = -0%, -5%
  halign = center
  valign = top
  zindex = 5
}

# USER AVATAR
image {
    monitor =
    path = $MPRIS_IMAGE
    size = 180 # lesser side if not 1:1 ratio
    reload_time = 0 # seconds between reloading, 0 to reload with SIGUSR2
    rounding = 0
    border_size = 0
    reload_cmd =  # command to get new path. if empty, old path will be used. don't run "follow" commands like tail -F
    position = -0%, -15%
    halign = center
    valign = center
}

# INPUT FIELD
input-field {
    monitor =
    size = 200, 50 #!
    outline_thickness = 2
    dots_size = 0.3 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
    outer_color = $primary_container
    inner_color = $on_primary_container
    font_color = $primary_container
    fade_on_empty = false
    placeholder_text = Input Password... # Text rendered in the input box when it's empty.
    hide_input = false
    rounding = 8 # -1 means complete rounding (circle/oval)
    check_color = $on_primary
    fail_color = $on_error # if authentication failed, changes outer_color and fail message color
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> # can be set to empty
    fail_transition = 300 # transition time in ms between normal outer_color and fail_color
    capslock_color = -1
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false # change color if numlock is off
    swap_font_color = true # see below
    position = -0%, -40%
    halign = center
    valign = center
}

#shape {
#    monitor =
#    size = 40%,100%
#    color = $primary_container
#
#    halign = left
#    valign = top
#    zindex = 1
#}

#shape {
#    monitor = 
#    size = 35%,95%
#    color = $surface_container
#
#    halign = left
#    valign = center
#    position = 2.5%, 0
#    zindex = 2
#}


# USER Greeting
label {
    monitor =
    text = cmd[update:60000] $fn_greet
    color = $white
    font_size = 20
    font_family = $fontFamily
    position = -0%, -30%
    halign = center
    valign = center
}

# Mpris and SPLASH
label {
    monitor =
    text = cmd[update:1000] $SPLASH_CMD # Outputs the song title when mpris is available, otherwise, it will output the splash command.
    color = $white
    font_family = $fontFamily
    font_size = 15
    position = 0, 5
    halign = center
    valign = bottom
}



# Battery Status if present
label {
    monitor =
    text = cmd[update:5000] $BATTERY_ICON
    color = $primary
    font_size = 20
    font_family = $fontFamily
    position = -1%, 1%
    halign = right
    valign = bottom
}

# Current Keyboard Layout 
label {
    monitor =
    text = $LAYOUT
    color = $white
    font_size = 16
    font_family = CaskaydiaCove Nerd Font
    position = -3%, 1%
    halign = right
    valign = bottom
}
